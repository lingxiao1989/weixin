<template name="likesTemplate">
  <view class="ratings-container">
    <view class="ratings">
      <block wx:for="{{ratings}}" wx:for-item="i" wx:key="index">
        <image class="ratingLikes" wx:if="{{i}}" src="../../common/image/like.png"></image>
        <image class="ratingLikes" wx:else src="../../common/image/none_like.png"></image>
      </block>
    </view>
    <text class="ratings-score">{{score}}</text>
  </view>
</template>